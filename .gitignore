.DS_Store

.eslintcache
node_modules/

.env
.env.local
.env.production

*.log

.turbo/

tsconfig.tsbuildinfo
*.tsbuildinfo

# Build output
dist/

.vscode

# Local-only agent + workspace settings
.claude/settings.local.json
.humanlayer/workspace.local.json

# Riptide artifacts (cloud-synced)
.humanlayer/tasks/

# Ephemeral local scratch space (handoffs, local-only notes)
.scratch/

# Agent worktrees (see .agents/skills/implement); .claude/worktrees is the
# legacy location still used by the harness's auto-isolation
.worktrees/
.claude/worktrees/

# Local telemetry JSONL (see packages/observability; clean with `pnpm telemetry:clean`)
var/telemetry/
